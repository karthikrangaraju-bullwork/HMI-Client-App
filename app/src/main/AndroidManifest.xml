<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission to bind to the server service -->
<!--    <uses-permission android:name="com.example.myhmimiddlewareapplication.BIND_HMI_DATA_SERVICE"/>-->

    <uses-permission android:name="com.bullwork.hmi_headless.BIND_HMI_DATA_SERVICE"/>
    <!-- Allows the client to see the server app on Android 11+ -->

    <queries>
        <package android:name="com.bullwork.hmi_headless"/>
    </queries>
<!--    <queries>-->
<!--        <package android:name="com.example.myhmimiddlewareapplication"/>-->
<!--    </queries>-->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HMIClientApp">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.HMIClientApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>
</manifest>
